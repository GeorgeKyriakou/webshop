import styled, { css } from "styled-components";

export const Navigation = styled.div`
  ${({ theme }) => css`
    width: 100%;
    height: 10%;
    display:flex;
    flex-direction:row;
    } */
  `}
`;